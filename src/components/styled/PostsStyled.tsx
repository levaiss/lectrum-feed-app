import styled, { type IStyledComponent } from 'styled-components'

export const PostsStyled: IStyledComponent<any, any> = styled.footer`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 10px 0;
  background-color: var(--paletteColor3);
`
